name: agent-apply-workflow-redesign

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - agent/evidence-workflow-manifest

permissions:
  contents: write

jobs:
  apply:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "bootstrap upload in progress"
